import React from "react";

const Header = () => {
    return (
        <header className="bg bg-red-500">
            <div className="bg-black">
            <h1>Welcome to the Blog!</h1>
            <p>This is a simple blog created with React.</p>
            </div>
        </header>
    );
};


export default Header;